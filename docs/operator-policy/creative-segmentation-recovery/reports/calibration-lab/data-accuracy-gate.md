# Creative Segmentation Calibration Lab - Data Accuracy Gate

Last updated: 2026-04-23 by Codex

## Result

Blocked. Calibration must not proceed to media-buyer judgment yet.

The corrected lab helper produced a sanitized artifact at:

`docs/operator-policy/creative-segmentation-recovery/reports/calibration-lab/artifacts/sanitized-calibration-dataset.json`

Gate result from the artifact:

- Gate passed: false
- Historical snapshot candidates inspected: 8
- Currently eligible candidates: 8
- Skipped candidates: 0
- Sampled eligible candidates: 3
- Verifiable sampled rows exported: 24
- Active eligible zero-row candidates: 1
- Table vs Decision OS identifier mismatches on exported rows: 0
- Max metric deltas on exported rows: 0 for spend, value, ROAS, CPA, purchases, impressions, and link clicks
- Blocking issue: `company-01` returned zero verifiable current Decision OS rows after eligibility filtering

The prior candidate-selection concern was real in code: the helper sampled from historical `meta_creatives_snapshots` before checking current Meta connection eligibility. The hardened helper now requires a connected Meta integration, non-empty access token, and assigned Meta account context before a historical snapshot business can be sampled.

In this rerun, all historical candidates were currently eligible, so the remaining zero-row case is a real Data Accuracy Gate blocker, not an ineligible-business sampling artifact.

## Candidate Eligibility

Eligibility filters:

- skip missing current Meta connection
- skip non-connected Meta connection status
- skip missing access token
- skip missing assigned Meta accounts

Skipped candidates by sanitized reason:

- `no_current_meta_connection`: 0
- `meta_connection_not_connected`: 0
- `no_access_token`: 0
- `no_accounts_assigned`: 0

## What Was Verified

- Creative table and Decision OS use the same current creative source path for exported rows.
- Exported rows use consistent creative identifiers between table rows and Decision OS rows after sanitization.
- Recent 7-day, mid 30-day, and long 90-day windows were fetched with fixed date windows from the source path, not by treating the selected UI reporting range as action authority.
- Account-relative baselines are present on exported rows and include reliability metadata.
- Campaign-relative baseline summaries in the artifact are lab-computed only and are not treated as production campaign benchmark authority.
- Commercial Truth missing/full state is carried on exported rows through `commercialTruthAvailability`.
- Old-rule challenger output is generated by `buildCreativeOldRuleChallenger` and is not delegated Decision OS output.
- Missing data is emitted as missing evidence or gate blockers instead of being silently treated as a valid judgment dataset.

## Remaining Blockers

- `company-01` is currently eligible but returned zero current Decision OS rows.
- `meta_creative_daily` has 0 rows, so warehouse-level independent creative fact verification is unavailable.
- Current verification confidence is API/payload parity only.

## Warehouse Fact Status

`meta_creative_daily` is not required for this gate because the current Creative product pipeline uses the creative API/snapshot source. The empty table is therefore a confidence limitation, not the immediate pass/fail blocker.

If `meta_creative_daily` is expected to be populated for future Creative source validation, that should be tracked as a separate source-health issue.

## Decision

Stop calibration at Phase B. Do not run the 10 media-buyer-agent panel until the active eligible zero-row source issue is resolved or classified with a precise source-health reason.
