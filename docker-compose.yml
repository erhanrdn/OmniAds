services:
  web:
    image: ${APP_IMAGE_REPO:-adsecute}-web:${APP_IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
      target: web-runner
      args:
        APP_BUILD_ID: ${APP_BUILD_ID:-dev-build}
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      APP_BUILD_ID: ${APP_BUILD_ID:-dev-build}
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
    ports:
      - "127.0.0.1:3000:3000"
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - >
          fetch('http://127.0.0.1:3000/api/build-info').then((response) => {
            if (!response.ok) process.exit(1);
          }).catch(() => process.exit(1))
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 30s

  worker:
    image: ${APP_IMAGE_REPO:-adsecute}-worker:${APP_IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
      target: worker-runner
      args:
        APP_BUILD_ID: ${APP_BUILD_ID:-dev-build}
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      APP_BUILD_ID: ${APP_BUILD_ID:-dev-build}
      NODE_ENV: production
      SYNC_WORKER_MODE: "1"
    depends_on:
      - web
    healthcheck:
      test:
        - CMD
        - node
        - --import
        - tsx
        - scripts/sync-worker-healthcheck.ts
        - --provider-scope
        - meta
        - --online-window-minutes
        - "5"
        - --min-online-workers
        - "1"
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 45s
